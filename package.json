{
  "private": true,
  "name": "rms-gcs-worker",
  "version": "1.0.0",
  "description": "Download files from GCS using CF Worker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "Otto Söderholm <otto.soderholm@iki.fi>",
  "license": "MIT",
  "devDependencies": {
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "aws4fetch": "^1.0.13"
  }
}
